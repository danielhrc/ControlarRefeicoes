<%= form_with(model: user_has_refeicao) do |form| %>
  <% if user_has_refeicao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_has_refeicao.errors.count, "error") %> prohibited this user_has_refeicao from being saved:</h2>

      <ul>
        <% user_has_refeicao.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :refeicao_id %>
    <%= form.text_field :refeicao_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
